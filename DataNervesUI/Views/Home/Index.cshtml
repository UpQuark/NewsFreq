@using System.Web.Mvc
@using System.Web.Mvc.Html
@model DataNervesUIMVC.Models.QueryModel

@{
    ViewBag.Title = "NewsLibrary Search";
}
@section featured {
    <section class="featured">
        <div class="content-wrapper">
            <hgroup class="title">
                <h2>@ViewBag.Title.</h2>
                <h2>@ViewBag.Message</h2>
            </hgroup>
        </div>
    </section>
}
<br/>

<div>
    @using (Html.BeginForm("Search", "Home", FormMethod.Get))
    {
        <div><span>Enter search term or terms. Boolean operators are supported</span><br/>
            @Html.TextBoxFor(m => m.SearchString, new { @placeholder = "Search terms" }) <br/>

            <span> Enter date from and date to </span><br/>
            @Html.TextBoxFor(m => m.DateFrom, new { @placeholder = "", @class = "DatePicker" }) <span>&nbsp; to &nbsp;</span>
            @Html.TextBoxFor(m => m.DateTo, new { @placeholder = "", @class = "DatePicker" })
            <br/>
    
            <span> Choose search target</span><br/>
            @*Html.DropDownListFor(m => m.SearchTargetId, new SelectList(Model.SearchTargets, "Value", "Text"))*@

            <input type="submit" value="Search" />
        </div>
    }
       
</div>

@section Scripts{
    @*@Scripts.Render("~/bundles/jqueryval")*@
    <script type="text/javascript">
        $(document).ready(function () {
            $(".DatePicker").datepicker();
        });
    </script>
}